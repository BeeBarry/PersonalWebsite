---
title: 'Skriv din första Dockerfile'
description: 'Från kommandon till en image som går att lita på'
category: 'docker'
part: 1
partTitle: 'Grunderna'
order: 2
date: '2026-08-04'
tags: ['Beginner', 'Dockerfile']
---

## Var vi är

I förra delen körde Lisa en färdig image som någon annan hade byggt, och lärde sig skillnaden
mellan imagen och containern.

Nu ska hon paketera sin **egen** applikation. Då räcker det inte att veta vad en container är —
hon behöver skriva ner *hur* den byggs.

## Receptet, inte maten

Det är precis vad en `Dockerfile` är: ett recept. En textfil med instruktioner som beskriver
vad som ska finnas i imagen och hur applikationen startas. Kör du `docker build` läser Docker
receptet uppifrån och ner och lagar maten — resultatet blir en image.

Skillnaden mot att installera saker för hand är att receptet går att läsa, granska och köra
om. Ingen behöver minnas vilka kommandon som kördes en fredagseftermiddag för ett halvår sedan.

<Flow
  caption="Två kommandon, tre tillstånd. Receptet är det enda du skriver för hand."
  steps={[
    {
      label: "Dockerfile",
      note: "Receptet. En textfil i ditt repo, granskad som all annan kod.",
    },
    {
      label: "Image",
      via: "docker build",
      note: "Den färdiga ritningen. Oföränderlig, taggad, delbar via ett registry.",
      accent: true,
    },
    {
      label: "Container",
      via: "docker run",
      note: "En körande kopia. Kastas och startas om fritt.",
    },
  ]}
/>

## En Dockerfile rad för rad

Här är en minimal Dockerfile för en Node-applikation. Fem rader, och varje rad gör en sak:

```dockerfile
FROM node:22-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --omit=dev
COPY . .
CMD ["node", "server.js"]
```

- **`FROM`** väljer utgångsläget. Du börjar aldrig från ingenting, utan från en befintlig
  image — här en Node-installation ovanpå Alpine Linux. `alpine` är en avskalad
  Linux-distribution, vilket gör imagen betydligt mindre.
- **`WORKDIR`** sätter arbetskatalogen inuti containern. Allt som följer sker där.
- **`COPY`** flyttar in filer från din dator till imagen.
- **`RUN`** kör ett kommando **när imagen byggs** — här installationen av beroenden.
- **`CMD`** anger vad som ska köras **när containern startar**. Den körs alltså inte vid bygget.

<Callout type="varfor" title="RUN eller CMD — den vanligaste förväxlingen">
`RUN` hör till **bygget**: det körs en gång, medan imagen skapas, och resultatet fryses in i
den. `CMD` hör till **körningen**: det körs varje gång en container startas från imagen.

En bra kontrollfråga när du är osäker: *ska det här ha hänt innan imagen är färdig?* Ja → `RUN`.
Nej → `CMD`.
</Callout>

## Varje instruktion blir ett lager

Titta på raderna igen. Varför kopieras `package.json` för sig, innan resten av koden?

Svaret är **lager**. Varje instruktion i en Dockerfile skapar ett nytt lager i imagen, och
Docker sparar dem. Bygger du om och ingenting har ändrats i ett lager återanvänds det direkt
i stället för att köras om — det kallas cache.

<Stack
  caption="Cachen faller vid första lagret som ändrats — och allt ovanför det måste byggas om."
  ground="Docker Engine"
  layers={[
    {
      label: "CMD [\"node\", \"server.js\"]",
      note: "Ändrar bara vad som startas. Kostar ingenting att bygga om.",
    },
    {
      label: "COPY . .",
      note: "Din källkod. **Ändras varje gång du sparar** — här faller cachen, nästan alltid.",
      accent: true,
    },
    {
      label: "RUN npm ci --omit=dev",
      note: "Installationen. Dyr att köra om — men behöver bara göras när raden under ändrats.",
    },
    {
      label: "COPY package*.json ./",
      note: "Beroendelistan. Ändras sällan.",
    },
    {
      label: "FROM node:22-alpine",
      note: "Basen. Hämtas en gång och delas mellan alla dina images.",
    },
  ]}
/>

Beroendena i `package.json` ändras sällan. Källkoden ändras hela tiden. Genom att kopiera in
`package.json` och installera beroendena **innan** koden kopieras, får `npm ci` behålla sin
cache så länge du inte lagt till ett nytt paket.

<Split
  leftTitle="Koden först"
  rightTitle="Beroendena först"
  caption="Samma fem instruktioner. Bara ordningen skiljer — och den avgör om bygget tar sekunder eller minuter."
>
  <div slot="left">

  ```dockerfile
  COPY . .
  RUN npm ci --omit=dev
  ```

  En enda ändrad bokstav i koden invaliderar `COPY`-lagret — och därmed allt ovanför.

  `npm ci` körs om **varje gång**. Bygget går från sekunder till någon minut, hela dagen.

  </div>
  <div slot="right">

  ```dockerfile
  COPY package*.json ./
  RUN npm ci --omit=dev
  COPY . .
  ```

  Kodändringen träffar bara det sista lagret.

  `npm ci` återanvänds ur cachen tills du faktiskt lägger till ett paket.

  </div>
</Split>

**Tumregel:** lägg det som ändras sällan högt upp i filen, och det som ändras ofta längst ner.

## Ta inte med mer än du behöver

`COPY . .` kopierar in allt i katalogen — inklusive `node_modules`, `.git`, lokala
loggfiler och den där `.env`-filen med hemligheter. Det gör imagen onödigt stor, och i värsta
fall läcker du något du inte tänkt.

Lösningen är en `.dockerignore`, som fungerar precis som en `.gitignore`:

```
node_modules
.git
.env
*.log
```

<Callout type="fallgrop">
Den här filen är lätt att glömma och märks inte förrän någon frågar varför imagen är 900 MB.

Värre är att en `.env` som kopierats in i ett lager **finns kvar i imagen** även om ett senare
lager raderar den — precis som en committad hemlighet finns kvar i Git-historiken. Den som
hämtar imagen kan gräva fram den.
</Callout>

## Bygg och kör

```bash
docker build -t min-app:1.0 .
docker run -p 3000:3000 min-app:1.0
```

`-t` sätter ett namn och en tagg (`min-app:1.0`), så du kan referera till imagen efteråt.
Punkten på slutet är **byggkontexten** — katalogen vars innehåll skickas till Docker. Den
glöms ofta bort, men det är den som avgör vad `COPY` faktiskt kan komma åt.

`-p 3000:3000` kopplar port 3000 på din dator till port 3000 inuti containern. Utan den
snurrar applikationen på i containern utan att någon kan nå den.

## En vanlig fälla: `latest` är inget löfte

<Callout type="fallgrop">
Det är frestande att skriva `FROM node:latest`. Problemet är att `latest` inte betyder
"senaste stabila" — det är bara en tagg som pekar på vad ägaren senast råkade publicera.

Bygger du om samma Dockerfile om tre månader kan du få en helt annan Node-version, och
applikationen som fungerade i går slutar fungera i dag, utan att du ändrat en rad kod.

Sätt en version du valt medvetet: `node:22-alpine`. Då är bygget **reproducerbart** — samma
recept ger samma resultat, oavsett när det körs.
</Callout>

<Callout type="kom-ihag">
- En `Dockerfile` är ett recept: instruktioner för hur en image byggs.
- `RUN` körs vid bygget, `CMD` när containern startar.
- Varje instruktion blir ett lager som cachas. **Lägg det som ändras sällan överst.**
- `.dockerignore` håller imagen liten och hemligheterna kvar på din dator.
- Lås versionen i `FROM` — `latest` gör bygget oförutsägbart.
</Callout>

<Check
  items={[
    {
      q: "Vad är skillnaden mellan `RUN` och `CMD`?",
      a: "`RUN` körs när imagen byggs och resultatet fryses in i ett lager. `CMD` körs när en container startas från imagen. Kontrollfrågan: ska det ha hänt innan imagen är färdig? Ja → `RUN`.",
    },
    {
      q: "Varför kopieras `package.json` in före resten av koden?",
      a: "För att skydda cachen. Beroendena ändras sällan, koden ofta. Installeras beroendena innan koden kopieras kan `npm ci` återanvändas ur cachen tills du faktiskt lägger till ett paket — annars körs den om vid varje sparning.",
    },
    {
      q: "Du lägger till `.env` i `.dockerignore` efter att ha byggt imagen en gång. Är hemligheten borta?",
      a: "Inte ur den redan byggda imagen — filen ligger kvar i det lager där den kopierades in, även om ett senare lager raderar den. Bygg om från grunden, och betrakta hemligheten som läckt om imagen delats.",
    },
    {
      q: "Vad är problemet med `FROM node:latest`?",
      a: "`latest` är bara en tagg som pekar på det senast publicerade, inte på en version du valt. Samma Dockerfile kan därför ge olika resultat vid olika tillfällen, och bygget slutar vara reproducerbart.",
    },
  ]}
/>

## Sammanfattning

En `Dockerfile` är receptet som gör en image reproducerbar: samma fil ger samma resultat, av
vem som helst, när som helst. `RUN` hör till bygget, `CMD` till körningen.

Varje instruktion blir ett cachat lager, och ordningen mellan dem avgör hur mycket som måste
byggas om. Det som ändras sällan hör hemma överst.

I nästa del tittar vi på vad som händer när applikationen behöver mer än en container: en
databas bredvid, och hur de hittar varandra.
